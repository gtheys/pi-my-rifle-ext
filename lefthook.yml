pre-commit:
  parallel: true
  commands:
    biome:
      glob: "packages/**/*.{ts,tsx,js,jsx,mjs,json,jsonc,md,toml,yml,yaml}"
      run: bunx @biomejs/biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

pre-push:
  commands:
    check:
      run: bun run check
