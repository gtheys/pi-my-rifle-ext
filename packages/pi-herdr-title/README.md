# pi-herdr-title

Pi extension that renames the current [herdr](https://github.com/...) pane to `Pi - <cwd>` when a session starts, so panes are identifiable in the workspace.

No configuration. No-op when pi runs outside herdr.
